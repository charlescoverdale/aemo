% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{aemo_fcas_enablement}
\alias{aemo_fcas_enablement}
\title{FCAS enablement volumes by generator unit}
\source{
AEMO NEMweb DISPATCHIS_Reports, DISPATCHLOAD table.
}
\usage{
aemo_fcas_enablement(
  duid = NULL,
  start,
  end,
  service = NULL,
  intervention = FALSE
)
}
\arguments{
\item{duid}{Optional character vector of DUIDs. \code{NULL} returns
all units. See \code{\link[=aemo_units]{aemo_units()}} for the full DUID registry.}

\item{start, end}{Window (inclusive), character or POSIXct.}

\item{service}{Optional character vector of service names,
e.g. \code{c("raise6sec", "lowerreg")}. Case-insensitive.
\code{NULL} returns all ten services.}

\item{intervention}{Logical. Default \code{FALSE}.}
}
\value{
An \code{aemo_tbl} with columns \code{settlementdate}, \code{duid},
and one column per FCAS service (\code{raise1secmw},
\code{lower1secmw}, \code{raise6secmw}, \code{lower6secmw},
\code{raise60secmw}, \code{lower60secmw}, \code{raise5minmw},
\code{lower5minmw}, \code{raiseregmw}, \code{lowerregmw}). Values are
MW; zero means the unit was not enabled for that service.
}
\description{
Returns the MW enabled for each of the ten Frequency Control
Ancillary Services (FCAS) per DUID per 5-minute dispatch
interval from \code{DISPATCHLOAD}. This is the quantity side of
the FCAS market: how much each unit was enabled (dispatched)
to provide each service, as distinct from the price returned
by \code{\link[=aemo_fcas]{aemo_fcas()}}.
}
\details{
Ten services are active since 9 October 2023 when Very Fast
(\code{RAISE1SEC} / \code{LOWER1SEC}) commenced. Before that date only
eight services are present; the \code{raise1secmw} / \code{lower1secmw}
columns will be \code{NA} for intervals before that date.
}
\examples{
\donttest{
op <- options(aemo.cache_dir = tempdir())
try({
  now <- Sys.time()
  e <- aemo_fcas_enablement(start = now - 3600, end = now)
  head(e)
})
options(op)
}
}
\seealso{
Other dispatch: 
\code{\link{aemo_bids}()},
\code{\link{aemo_constraints}()},
\code{\link{aemo_dispatch_units}()},
\code{\link{aemo_interconnector}()},
\code{\link{aemo_rooftop_pv}()}
}
\concept{dispatch}
